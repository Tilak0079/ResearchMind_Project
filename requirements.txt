# --- Web framework & API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
websockets==13.1
python-multipart==0.0.9

# --- Agent orchestration ---
langgraph==0.2.28
langchain-core>=0.2.39,<0.4    # range, not pin - lets pip pick a version langgraph is happy with

# --- Vector DB ---
qdrant-client==1.11.2

# --- PDF parsing ---
docling==2.4.0

# --- Tokenization (chunk sizing) ---
tiktoken==0.8.0

# --- Embeddings + Reranker (BAAI/bge-m3 + BGE-reranker-v2-m3, via official library) ---
# NOTE: originally planned FastEmbed for embeddings, but FastEmbed does not
# support bge-m3 in any version (confirmed by checking its full supported-
# model list directly). Switched to FlagEmbedding (BGE-m3's own official
# library) - it handles both embedding (Phase 6) and reranking (Phase 7).
FlagEmbedding>=1.2.0
huggingface_hub>=0.23,<1.0     # pinned down - FlagEmbedding wants 1.x but Docling needs <1.0
pyarrow>=16.1.0,<17.0.0        # pinned down - Docling needs this range, FlagEmbedding's datasets dep pulls newer

# --- Database ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.3

# --- Cache / Queue ---
redis==5.0.8
celery==5.4.0

# --- Object storage ---
boto3==1.35.24          # S3-compatible client, works with MinIO too

# --- Frontend ---
streamlit==1.38.0

# --- Config & utils ---
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.5.2

# --- PDF sanitization (Phase 8) ---
pikepdf==9.3.0

# --- LLM client (talks to Ollama's OpenAI-compatible API) ---
openai==1.51.0
httpx==0.27.2    # pinned - newer httpx (0.28.x) removed a param openai 1.51.0 still expects

# --- Frontend WebSocket client (talks to our own FastAPI WebSocket) ---
websocket-client==1.8.0